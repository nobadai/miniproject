# Browser에 공개 가능한 Backend API 주소
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Server 전용: Docker Compose 등 Container 환경에서 Server Component가 Backend를
# 호출할 때 쓰는 Container 간 통신 주소. Local(Docker 없이 npm run dev)에서는
# 비워두면 NEXT_PUBLIC_API_BASE_URL로 자동 대체된다.
BACKEND_INTERNAL_URL=
